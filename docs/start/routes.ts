/*
|--------------------------------------------------------------------------
| Routes file
|--------------------------------------------------------------------------
|
| The routes file is used for defining the HTTP routes.
|
*/

// router.get('/docs', [controllers.Docs, 'handle']).as('docs')
// router.get('/docs/*', [controllers.Docs, 'handle']).as('docs.slug')
